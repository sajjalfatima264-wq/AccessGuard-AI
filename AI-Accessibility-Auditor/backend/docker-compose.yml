version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - FRONTEND_URL=http://localhost
      - OPENAI_API_KEY=sk-mock
  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
